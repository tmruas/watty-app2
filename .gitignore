__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env
.env.*
!.env.example

# Streamlit: secrets are local only — if this file was ever committed, run:
#   git rm --cached .streamlit/secrets.toml
.streamlit/secrets.toml

# Login wizard: ignore only npm deps. The pre-built bundle under
# `watty_login_wizard/frontend/build/` must stay committed for Streamlit Cloud
# (no `npm run build` there).
watty_login_wizard/frontend/node_modules/

# Next.js (Watty web)
web/node_modules/
web/.next/
web/.env*.local
